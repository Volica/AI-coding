# 9.3 习题三：组合数学与模运算求解器

这一题来自 `test/math` 文件夹。它比前两题更接近传统算法题：读者需要补全 `solve(n, k, m)`，计算整数划分方案数，再乘以 `m!`，最终对 `10^9 + 7` 取模。

![组合数学练习](assets/9-3-1.png)

## 题目要点

| 规则 | 要求 |
| --- | --- |
| 整数划分 | 计算 `n` 个相同物品划分为 `k` 个非空组的方案数 |
| 排列乘法 | 将划分方案数乘以 `m` 个不同物品的全排列数 `m!` |
| 模运算 | DP 转移和阶乘乘法都要及时对 `10^9 + 7` 取模 |
| 空间优化 | 使用一维动态规划，避免二维表造成不必要内存 |
| 边界处理 | `k > n` 返回 0，`k == n` 时划分方案数为 1 |
| 性能要求 | 大输入下仍能在测试限制内完成 |

## 练习标签

```sbs-exercise
key: partition-factorial-solver
title: 组合数学与模运算求解器
kind: completion
language: python
judgeMode: testCases
description: |
  补全 solve(n, k, m)。

  目标：
  1. 计算将 n 个相同物品划分为 k 个非空组的方案数。
  2. 将方案数乘以 m!。
  3. 最终结果对 10^9 + 7 取模。

  要求：
  - 使用一维动态规划。
  - DP 每次加法转移后取模。
  - 阶乘每次乘法后取模。
  - 处理 k > n、k == n 等边界。

  公开样例：
  | 输入 | 期望输出 |
  | --- | --- |
  | solve(5, 2, 3) | 12 |
code: |
  def solve(n, k, m):
      MOD = 10**9 + 7

      # BEGIN_SOLUTION
      if k > n:
          return 0

      remaining = n - k
      dp = [0] * (remaining + 1)
      dp[0] = 1

      for part in range(1, k + 1):
          for total in range(part, remaining + 1):
              dp[total] = (dp[total] + dp[total - part]) % MOD

      factorial = 1
      for value in range(2, m + 1):
          factorial = (factorial * value) % MOD

      return (dp[remaining] * factorial) % MOD
      # END_SOLUTION
judgeTests: |
  import time

  CASES = [
      (5, 2, 3, 12),
      (6, 3, 4, 72),
      (10, 4, 5, 1080),
      (20, 5, 8, 3386880),
      (1000, 500, 99999, 578108381),
  ]

  start = time.perf_counter()
  for n, k, m, expected in CASES:
      check(f"solve({n}, {k}, {m})", solve(n, k, m), expected)
  elapsed = time.perf_counter() - start
  check_true("执行时间应小于 1 秒", elapsed < 1.0)
```
