
```markdown

书籍内容：

> 

大纲产生思路：

> 见thinking.md

技术支持：

> 见technique.md

第零部分：新书导入

> 见book_introduction.md

项目实践：

> 见test文件夹下对应

```

# 《AI 辅助编程实战》大纲

# 上篇：打通vibe coding全流程

**主线贯穿：从零开发“网页版五子棋”小程序**

*通过一个可视化的网页小游戏，带读者跑通无需配置本地环境的 AI Coding 基础闭环。重点是先把开发、运行、修改、验证这条线走通，而不是一开始就陷入环境配置的难题。*

基本流程：用户跟着教程生成五子棋网页文件（chess.html），下载到本地，用浏览器运行即可。

基本功能需求：（见“五子棋.md”）

网页版五子棋的效果：（运行"chess.html"）


进阶工具（部署到公网）：即静态网站托管服务：

* github pages服务
* 900.cool网站：[https://www.900.cool/](https://www.900.cool/)

<!-- ## 第一部分：基础设施与需求表达 -->

### 第1章 重新定义编程与 AI 边界 (Redefining Programming and AI Boundaries)

**绑定实践：五子棋项目的启动阶段。** 从“我要一个网页版五子棋”出发，明确人负责需求、验收和取舍，AI 负责协助生成、解释和修改。

#### 1.1 编程范式转换：从只写代码到设计、审查与验收

#### 1.2 AI 在软件开发中的优势

AI 在软件开发中的主要价值，是在目标、上下文、接口和验证标准相对明确时，提高局部任务的完成效率。它不是独立的软件负责人，而是可以快速执行、快速反馈的协作者。

AI 的优势主要体现在以下方面：

1. **快速完成边界明确的实现任务**  
   当输入输出、接口契约、数据结构和验收标准已经明确时，AI 可以高效生成样板代码、业务函数、测试草稿、文档草稿和重构方案。

2. **辅助方案比较和技术讨论**  
   在需求相对清楚的情况下，AI 可以提出多种实现思路，帮助开发者比较复杂度、可维护性、扩展性和潜在风险。

3. **处理重复性和模式化工作**  
   对于类型定义、接口封装、数据转换、错误处理、配置生成、基础测试等结构清晰的任务，AI 通常能提供稳定的辅助。

4. **辅助理解代码和定位问题**  
   AI 可以帮助解释已有代码、梳理调用链、分析错误日志、推测可能原因，并提醒开发者关注边界情况。

5. **支持文档维护和经验沉淀**  
   AI 可以把零散记录整理成结构化文档，也可以辅助生成接口说明、验收步骤、变更记录和协作规范。

6. **执行清晰目标下的重构任务**  
   当人类已经指出问题和重构目标时，AI 可以较好地执行模块拆分、命名整理、重复代码消除和局部结构优化。


#### 1.3 AI 在软件开发中的局限

AI 不能被视为项目责任主体，也不能替代人类完成关键判断。它的输出必须经过审查、验证和必要的修正。

AI 的局限主要包括：

1. **不能承担需求和责任判断**  
   AI 不真正理解项目的业务责任、课程要求、用户风险和长期维护成本，不能替代人类决定“应该做什么”“为什么做”以及“做到什么程度”。

2. **容易受上下文和已有代码影响**  
   AI 会模仿当前代码风格，包括不良模式。如果仓库中存在命名混乱、重复代码或临时写法，AI 可能继续复制并放大这些问题。

3. **可能生成不准确或不可运行的内容**  
   AI 可能编造不存在的 API，误解项目上下文，忽略已有架构约束，或者给出看似合理但无法通过验证的实现。

4. **不能天然保证工程质量**  
   AI 生成的代码不必然满足安全性、隐私保护、性能、可维护性和可测试性要求。涉及用户数据、密钥、网络请求、文件读写和权限控制时，必须由人类严格审查。

5. **对复杂环境和依赖问题判断不足**  
   当问题来自框架缺陷、版本差异、运行环境、第三方依赖或系统级行为时，AI 可能倾向于在现有代码里绕过问题，而不是识别真正的外部约束。

6. **对并发、异步和跨模块协同问题更容易失误**  
   多线程、多进程、异步任务、资源释放、状态同步等问题通常难以复现和验证，AI 在这类场景中需要更明确的日志、测试和人工判断。

7. **缺乏主动的领域洞察和产品判断**  
   AI 能识别现象，但不一定能判断某个“正常现象”是否应该优化。领域知识、工程直觉和产品取舍仍然是人类的核心责任。

8. **不能替代最终验收**  
   架构取舍、隐私边界、数据结构设计、用户体验判断和最终验收必须由人类负责。AI 可以辅助分析和生成方案，但不能代替项目成员承担最终决策。

因此，在 AI 协作开发中，更合理的定位是：**AI 是高效率协作者，不是项目负责人；人类是定方向、审代码、做判断的驾驶员。**


#### 1.4 人机协作

* 核心原则：人类定需求与验收，AI 负责完成中间实现。
* **引入自我审查机制：** 要求 AI 在产出后先做一次 Self-Review，核对代码是否符合初始需求，例如生成完代码后先检查一遍主要逻辑。

* 结合五子棋：人类负责测试游戏体验与寻找 Bug，AI 负责修复 HTML/CSS/JS 逻辑缺陷

* AI 优劣可结合五子棋：AI 擅长写棋盘 UI 与胜负判断，但不适合直接承担复杂对战策略设计。

### 第2章 AI 工具解析 (AI Tooling Overview)

**绑定实践：五子棋项目的工具选择阶段。** 对比浏览器对话、Agent/CLI 和网页编辑环境，确定先用轻量工具生成 `chess.html`，再按需要进入本地或托管流程。

按照课堂框架，本章将工具分为三层：

1. **模型层：负责理解、生成与推理**
   * 对话式大模型：ChatGPT / DeepSeek / Claude / Kimi 等
   * 重点说明：模型能力差异、上下文长度、代码生成稳定性、中文需求理解、推理与执行成本

2. **Agent / Harness Tool 层：负责把模型接入工程流程**
   * Agent 工具：Codex / Claude Code / OpenCode / Kimi CLI 等
   * IDE 集成：Cursor / Trae / VS Code 插件等
   * 重点说明：读取项目上下文、修改文件、运行命令、执行测试、根据日志继续修复

3. **其他工具层：负责运行、部署、协作与验证**
   * 版本管理：Git / GitHub
   * 浏览器与调试工具：DevTools、截图、控制台日志
   * 测试与辅助脚本：Python、Shell、静态检查工具

* 最后简单结合五子棋：对于简单网页项目，先选择能快速生成单文件页面的模型或网页端工具；当需要多轮修改、测试、部署时，再引入 Agent/CLI、浏览器调试和静态托管工具。

### 第3章 从需求到计划 (From Requirements to Plan)

---
3.0
**绑定实践：五子棋项目的需求拆解阶段。** 把“做一个五子棋”拆成棋盘绘制、落子交互、胜负判定、重新开始、样式反馈等可验收任务。

首先，正式介绍五子棋项目，只需要先向读者介绍我们要实现一个网页版的五子棋，同时具有人机对弈的功能和悔棋、认输、重新开始的功能即可，其文件具体在test/chess下面。
这个五子棋网页**所有功能都能在一个 HTML 文件里完成，无需额外库，体验自然不简陋。**
- **悔棋**：点击按钮可以悔一步（同时撤销你和 AI 的上一步，回到你走棋前的状态）。可以连续悔棋到开局。
- **重新开始**：一键清空棋盘，准备新的一局。
- **认输**：点击认输按钮，当前对局结束，AI 获胜。

-----
3.1 开始前指定计划，明确边界

* 伪代码驱动业务逻辑
* AI 编程的起点不是写代码，是写文档

3.1.1 需求澄清与规划前置（核心法则）：**
* **文档优先与需求澄清：** 拒绝含糊其辞，提供具象化的验收指标。先让 AI 追问并澄清需求边界，将需求具象为 PRD 或说明文档，而非零散的聊天记录。
* **先设计后编码 (Plan Mode)：** 严禁让 AI 直接生成代码。优先进行架构设计与复杂任务拆解，人工确认规划无误后再进入执行环节，降低返工率。
---
3.1.2 **规范产出结构：** 在任务初期就约定好输出的格式与代码结构，确保最终产出易于人工追溯与审查。

----
3.1.3 任务切片
* 任务切片与模块化拆解
* 任务切片：将五子棋拆解为“绘制棋盘”、“落子交互”、“胜负判定”三大模块
-----


3.2 AI Coding Prompt 框架 (AI Coding Prompt Framework)

**绑定实践：五子棋项目的提示词编写阶段。** 用结构化 Prompt 明确页面形态、交互规则、输出文件、禁止项和验收标准。

* CRISPE 框架应用（角色/任务/约束/格式）
* 不要只给模糊的需求，要给具体的指标和完成定义


* Few-Shot 示例投喂：让 AI 模仿特定的棋盘 CSS 风格与视觉反馈
* prompt训练：对比随意的/有框架的prompt，做几个关于五子棋网页的小练习题，【前后端】在水杉智能学习平台查看代码，在AI助手实时查看效果


### 第4章 代码生成与审查 (Code Generation and Review)

**绑定实践：五子棋项目的首版生成与代码审查阶段。** 让 AI 生成 `chess.html` 后，重点检查棋盘渲染、落子状态、胜负判断和重开逻辑。

本章关注如何让 AI 辅助生成代码，以及如何对 AI 生成的代码进行审查。AI 可以帮助生成函数、类、接口、页面组件、数据处理逻辑、测试草稿和重构方案，但生成结果不能直接等同于可合并代码。

要求：结合之前给出的提示词，进一步优化（可以是整段提示词的优化，也可以是记忆之前的提示词，对部分功能进一步优化）

本章建议包括：

---
4.1
- 明确生成目标：说明要实现什么功能、输入输出是什么、不能修改哪些模块。
- 提供上下文：给出相关接口、数据结构、现有代码风格和约束条件。
- 分批生成代码：优先让 AI 完成边界清晰的小任务，而不是一次生成整个系统。

---
4.2
- 审查正确性：检查代码是否满足需求，是否处理异常情况，是否存在明显逻辑漏洞。
- 审查一致性：检查命名、目录结构、接口格式、错误处理和代码风格是否与项目已有规范一致。
- 审查安全性：检查是否泄露密钥、是否直接信任用户输入、是否绕过权限或隐私约束。
- 审查可维护性：检查是否过度设计，是否引入不必要依赖，是否让简单逻辑变复杂。
- AI 会盲目复制代码风格，所以必须定期 code review

### 第5章 测试与检验 (Testing and Verification)

**绑定实践：五子棋项目的浏览器验收阶段。** 在浏览器中测试横、竖、斜胜利，重复落子、棋盘已满、重新开始等边界场景。

本章关注如何验证 AI 生成或修改后的代码是否真正可用。测试与检验不只包括自动化测试，也包括人工操作、边界输入、异常路径和回归验证。

本章建议包括：
---
5.1
- 编译与构建检查：确认项目能够正常编译、打包或启动。
- 类型与静态检查：使用项目已有工具检查类型错误、格式问题和潜在代码质量问题。
---
5.2
- 单元测试：验证单个函数、类或模块在不同输入下是否符合预期。
- 集成测试：验证多个模块组合后是否能够正确协作。
- 端到端测试：从用户视角验证一条完整操作流程是否成立。
- 边界场景测试：验证空数据、重复数据、无效输入、超长输入、网络失败、权限不足等情况。
---
5.3
- 手动验收记录：记录测试环境、操作步骤、输入数据、预期结果、实际结果和遗留问题。
- 验证方案是架构之外最重要的 harness 机制。

### 第6章 排错 (Debugging)

**绑定实践：五子棋项目的缺陷修复阶段。** 把“胜负判断不准”“按钮没反应”“样式错位”等问题转化为可复现步骤、控制台日志和预期结果。

本章关注如何借助 AI 更高效地定位和修复问题。排错时不能只描述“报错了”，而应尽量提供可复现信息，让 AI 能够基于事实分析问题。

本章建议包括：
---
6.1 找错误
- 收集错误信息：包括错误日志、报错截图、失败命令、状态码、异常堆栈等。
---
6.2 明确具体错误
- 复现问题：记录从正常状态到触发错误的具体步骤。
- 缩小范围：判断问题来自输入数据、业务逻辑、接口调用、依赖环境、配置文件还是外部服务。
---
6.3 修错误
- 检查最近改动：对比问题出现前后的代码变化，优先排查最近修改过的模块。
- 验证假设：一次只修改一个可能原因，避免同时改动过多导致无法判断真实原因。
- 修复后回归：确认原问题消失，并重新测试相关功能，避免修复一个问题又引入另一个问题。
- 反馈质量决定 AI 修复效率：不要只说有问题，要给日志、预期和复现步骤。

### 第7章 文档维护 (Documentation Maintenance)

**绑定实践：五子棋项目的交付整理阶段。** 将运行方式、功能清单、已知问题、测试记录和部署链接写回 README 或项目说明。

本章关注如何在 AI 协作开发中保持文档与代码同步。文档不是开发结束后的补充材料，而是需求澄清、任务分工、接口约束和后续维护的重要依据。

**新增**：
1、上下文过长，需要截断，保存截断记录的文档；
2、介绍如何命名文档，（根据功能+英文命名）；
3、经常归档保持对话清洁；
4、对于五子棋项目，给出诸如readme，需求，架构，接口，测试文档示例（请结合之前的五子棋的示例提示词，不要脱节）
5、文档需要详细，但不需要重复；

本章建议包括：

- README 维护：说明项目目标、运行方式、核心功能、目录结构和基本使用流程。
- 需求文档维护：记录功能目标、用户场景、边界条件和不做什么。
- 架构文档维护：记录模块划分、调用关系、数据流向和关键技术选择。
- 接口文档维护：记录接口名称、参数、返回值、错误格式和调用示例。
- 测试文档维护：记录自动化测试、手动验收步骤、测试数据和已知问题。
- 变更记录维护：说明每次重要修改的原因、影响范围和验证结果。
- 命名与归档：将临时记录整理成有意义的文件名，最好英文命名，避免长期保留含义不清的草稿文件。
- 记录关键决策和状态，不记录无价值过程细节，并在里程碑后更新文档。

### 第8章 AI 使用技巧 (AI Usage Techniques)

**绑定实践：五子棋项目的迭代优化阶段。** 在增加“悔棋”“计时”“重新开始”等功能时，练习上下文整理、分会话处理和多工具协作。

*本章结合主创团队的 AI coding 经验，介绍高阶心法与提效技巧。*

这章不需要提及五子棋

#### 8.1 上下文管理

* 长对话防截断策略：为五子棋增加“悔棋”功能时，如何高效传递历史代码
* LLM沟通每次都全部保存对话并发送，利用这个特性发现一些沟通技巧
* 沟通技巧：全量代码保存与局部更新的取舍
* **精准交互与会话优化：**
* **会话拆分与 Token 管理：** 按功能拆分会话，必要时 Fork 新分支，让每次对话都围绕当前任务展开。


#### 8.2 从对话工具到 Agent 协作

* **从 Chat 到 Agent：** 不只把 AI 当作问答工具，也可以让它在明确边界内读取文件、运行命令、执行测试并根据结果继续修改。

* **按任务选择工具：** 发挥不同模型和工具的优势，例如用擅长规划的模型拆任务，用 Agent/CLI 执行文件修改与测试；也可以通过 Claude Code/Codex 等工具进行远程流转与审批。

#### 8.3 沉淀个人工具箱

* **构建专属 AI 工具箱：** 持续沉淀个人的 Prompt 模板、代码审查规则和常用调试方法，让后续项目少走重复的弯路。
**常用工具积累**：自己工作中常用的功能或其中一环，比如文献查找、word转pdf、数据清洗等功能变成一个符合自己习惯的AI小工具

#### 8.4 多AI协助
- 尝试用多个AI帮忙做事，比如gemini帮忙发散思维，chatgpt实践，秘塔搜索；
- 多AI交叉实践验证，互相检测正确性等


### 第9章 实战 (Practice Cases)

**绑定实践：从五子棋的人工验收，过渡到可自动判定的 Prompt/代码测试。** 三个小案例分别训练格式约束、逻辑分支、性能边界和测试脚本设计。

*预设标准答案，通过 Test Case（测试用例）严格检验 AI 生成代码或 Prompt 的逻辑严密性与格式稳定性。*

#### 实战一：水果店计费系统

* **场景：** 编写一段高阶提示词（Prompt），让 AI 处理自然语言输入（如：“草莓3斤，需要果切，节日期间，需要环保袋”），并根据以下严格规则计算费用：
1. **阶梯与条件折扣：** 苹果8元/斤、草莓22元/斤、葡萄15元/斤。满3斤同种水果打9折并向下取整。
2. **附加服务费：** 果切费2元/斤（按总斤数计）。
3. **特殊溢价：** 节日期间，水果费与果切费合计后加收20%，并向上取整。
4. **固定耗材：** 环保袋2元。


输入：一段自然语言输入

输出：JSON输出，仅两个字段———仅输出包含 `total_bill`（最终总价）和 `fruit_cost`（节假日加价前的折后水果费）两个字段的标准 JSON 字符串，过滤任何多余的解释性废话（幻觉控制）。

* **检验与难点：** * **运算次序与精度控制：** 检验 AI 能否严格遵循“折扣向下取整 $\rightarrow$ 叠加服务费 $\rightarrow$ 溢价向上取整 $\rightarrow$ 叠加耗材”的先后顺序计算。

* **自动化测试：** 编写 Python 检验脚本，批量输入不同的顾客购买描述，解析返回的 JSON 并与人工预设的正确金额进行断言比对（Assert）。

---

### 实战二：网约车计费系统 (Practice II: Ride-hailing Fare Calculator)

* **场景：** 编写一段高阶提示词（Prompt），让 AI 处理包含时间、里程、优惠和附加费用的自然语言输入（如：“昨晚11点半打车回家，一共跑了14公里，交了10块钱过路费，账户里有一张满40减8元的券和一张75折券”），并根据以下严格规则计算最优费用：

1. **分段里程计费：** * 起步价 13元（包含前 3公里）。
* 超出起步里程部分：3 到 10公里（含）按 2.5元/公里计费；超过 10公里的远途部分按 3.5元/公里计费（远途加价）。


2. **时段浮动费率：** * 若出发时间处于夜间（23:00 - 06:00），计算出的**纯里程费**（不含起步价）需要加收 20% 的夜间服务费，且该部分计算结果**向上取整**。
3. **动态折扣（互斥原则）：** * 将起步价与最终里程费相加得到“基础车费”。
* 若用户同时拥有“满减券”和“折扣券”，系统需自动比较两种优惠方案，**强制选用对用户最划算（最终车费最低）的方案**。折扣后的车费**向下取整**。


4. **固定附加费：** * 高速费、停车费等附加费用**不参与任何折扣**，在最终扣款时直接原价叠加。

* **输入：** 一段包含打车里程、时间、路桥费及可用优惠券的自然语言描述。
* **输出：** 标准 JSON 字符串，仅包含以下三个字段，绝对过滤任何解释性废话：
* `total_fare`（最终需支付的总金额）
* `base_fare`（打折前的基础车费总额，不含附加费）
* `applied_discount`（实际生效的优惠策略，输出字符串如 "满40减8" 或 "75折"）


* **检验与难点：** * **逻辑分叉与寻优能力：** 检验 AI 是否具备“分支计算并比较”的能力（AI 必须在隐式思维过程中同时计算满减和打折的结果，比较大小后才能得出 `applied_discount`）。
* **作用域陷阱：** 检验 AI 是否能精准区分各类费用的作用域（夜间加价**仅**针对超出的里程费；打折**仅**针对基础车费，绝对不能把过路费也打折）。
* **自动化测试挑战：** 编写 Python 测试脚本，构造极具迷惑性的 Edge Case。例如：某一里程下，满减和打折的最终金额完全一致，测试 AI 的边界处理机制；或者总里程为 2.5公里（未超起步价），测试 AI 是否会错误计算负数里程。



#### 实战三：背包问题

* **场景：** 编写一段高阶提示词（Prompt），要求 AI 编写一个高效 Python 求解算法，解决经典的 0/1 背包问题（每一个物品有重量和价值，背包有重量上限，要求整个背包价值最大）：

1. **核心逻辑（物品选择与容量约束）：** 给定 $n$ 个物品，每个物品只能选择一次。第 $i$ 个物品有重量 $w_i$ 和价值 $v_i$，背包最大承重为 $C$，要求在总重量不超过 $C$ 的前提下，使选中物品的总价值最大。
2. **动态规划约束：** 要求 AI 正确定义状态转移关系，例如 `dp[c]` 表示容量不超过 `c` 时能够取得的最大价值；处理每个物品时必须对容量进行倒序遍历，避免同一个物品在一轮转移中被重复选择。
3. **性能要求：** 针对较大数据范围（如 $n \le 1000$、$C \le 10^5$），强制要求 AI 使用空间复杂度优化的一维滚动数组，将空间复杂度控制在 $O(C)$，避免使用 $O(n \cdot C)$ 的二维表导致内存压力过大。

* **输入：** 物品数量 `n`、背包容量 `C`，以及两个长度为 `n` 的列表 `weights` 和 `values`，分别表示每个物品的重量和价值。
* **输出：** 纯代码输出——仅输出一个名为 `solve(n, C, weights, values)` 的 Python 函数代码块，返回背包可获得的最大价值。强制要求过滤任何 Markdown 标记（如 ````python`）、导入语句以外的多余代码，以及解释性废话（幻觉控制）。
* **检验与难点：** * **状态转移与遍历方向：** 检验 AI 是否能写出正确的时间复杂度为 $O(n \cdot C)$ 的 DP 状态转移，并确认容量循环是否采用倒序遍历。若误用正序遍历，0/1 背包会退化成完全背包，导致同一物品被重复使用。
* **边界条件控制：** 检验 AI 是否能正确处理背包容量为 0、物品重量超过容量、存在 0 价值物品、多个物品重量相同但价值不同、所有物品都无法放入等情况。
* **自动化测试（OJ 判题模拟）：** 编写本地测试脚本（类似 LeetCode 的判题机），通过 `exec()` 或动态导入的方式加载 AI 生成的函数，批量传入典型用例和极端边界测试用例（如 $n=1000, C=100000$）。一方面使用断言（Assert）比对最大价值的正确性，另一方面严格限制并监控函数的执行时间（如必须在 1 秒内跑完）。
---

# 下篇：本地端 AI Coding 与工程实践

*脱离网页，深入本地 IDE 与终端，学习如何把 AI Coding 放进日常开发流程。*

**主线贯穿：本地文件管理工具**

特点：本地化部署、难度简单

* 本地文件系统管理（PowerShell/Python）
* API 调用与文件读写权限处理
* 平台提供测试文件

* **目标：** 让 AI 写一小段代码，自动帮你把桌面上乱七八糟的文件按类型（图片、文档、视频）分门别类丢进不同的文件夹。
* **学习点：** 体验 AI 如何操控本地文件，安全且实用。

## 第二部分：环境、调试与质量控制

### 第10章 核心环境与模型配置 (Core Environment and Model Configuration)

**绑定实践：本地文件管理工具的环境准备阶段。** 新建本地工作区，配置 Python/PowerShell 脚本环境，选择适合本地文件操作的模型与工具。

* 本地 IDE（Cursor / Trae）与 CLI 工具入门
* 主要教用户如何在本地使用 AI coding，环境配置，使用技巧，介绍如 Codex, Trae 等使用，模型的选取，模型的配置
* 结合项目：新建本地工作区、配置 Python/脚本环境与模型参数选择

### 第11章 项目上下文管理 (Project Context Management)

**绑定实践：本地文件管理工具的上下文传递阶段。** 让 AI 理解桌面文件样例、分类规则、目标目录结构和禁止误删的安全边界。

本地项目的规则往往不只写在一个文件里，而是分散在目录结构、配置文件、脚本入口、README、测试数据和历史命名中。让 AI 理解项目全貌，是为了让它知道哪些文件可以读、哪些目录不能动、分类规则从哪里来、输出结果应该落到哪里。否则 AI 只看单个脚本时，容易误判路径关系、依赖关系和安全边界，写出看似合理但会误移动、误覆盖甚至误删文件的代码。

* 目录结构 (Tree) 与依赖关系传递
* 外部文档引入与全局文件提醒技巧
* 结合项目：如何让 AI 准确理解你的项目文件夹与分类规则
* **上下文传递步骤：** 先给目录树，再给关键文件内容，再说明分类规则、禁止操作、测试目录和预期输出。
* **本地文件管理案例：** 让 AI 先理解测试文件夹、目标分类目录、文件类型映射和重名处理规则，再要求它编写或修改整理脚本。
* **工程化上下文与资产沉淀（项目篇）：**
* **提供真实环境上下文：** 让 AI 读取项目目录、配置、代码和历史规则，辅以代码内的清晰注释，减少只看局部片段就贸然修改的问题。
* **沉淀项目级资产：** 为每个项目配置专属的 AI 说明书（如 `rules.md`、`README.md`），明确架构和避坑指南。


### 第12章 代码审查与结果验证 (Code Review and Result Verification)

**绑定实践：本地文件管理工具的安全验收阶段。** 检查脚本是否只移动测试目录内文件，是否支持预览模式，是否能处理重名、空目录和未知类型。

* 核心逻辑与边界条件人工校验
* 安全审计与性能复杂度优化
* 自动化单元测试生成与代码简洁度优化
* **本地化审查流程：** 先确认需求是否被完整实现，再检查路径、权限和文件写入范围，随后验证边界条件，最后用测试目录跑一遍完整流程。
* **文件操作安全检查：** 检查脚本是否只处理指定测试目录，是否支持 dry-run/预览模式，是否会误删文件，是否会跨目录移动文件，是否能处理重名文件和未知扩展名。
* **结果验证方法：** 对比运行前后的测试文件夹快照，确认图片、文档、视频等文件进入正确目录，并记录未分类文件和异常文件的处理结果。
* **测试驱动与标准定义：**
* **测试先行与标准定义：** 在开发前写好测试用例和验收标准，让 AI 明确“何为完成”。


### 第13章 报错排查与终端调试 (Error Triage and Terminal Debugging)


**绑定实践：本地文件管理工具的终端调试阶段。** 通过命令输出、异常堆栈和测试文件夹结果，定位路径、权限、编码和分类规则错误。

* 终端 Error Log 与堆栈提取
* 逐行解析与依赖冲突排查
* **本地化调试流程：** 先复现失败命令，再保存终端输出和 Python traceback，接着判断错误属于路径、权限、编码、依赖还是分类规则问题，修复后重新运行同一组测试文件。
* **常用工具与证据：** 使用 PowerShell 查看当前路径和文件列表，使用 `tree` 或目录快照对比运行前后结构，保留日志文件、报错截图和测试文件夹状态，减少只用自然语言描述问题。
* **文件管理案例：** 遇到路径不存在、文件被占用、中文文件名乱码、扩展名识别错误等问题时，把失败文件名、运行命令、期望目录和实际结果一起交给 AI 分析。
* **打造全自动 Debug 闭环：**
* **机器直读 Logs：** 建立“修改-测试-读日志-修复”的自动化流转；遇到报错时直接提供 Logs 进行排查，效率远高于自然语言描述。
* **从源头修正错误：** 发现错误时，回到需求文档、规则或测试用例中修正，而不是只在当前对话里口头纠正。



### 第14章 测试台工程 (Harness Engineering)

**绑定实践：本地文件管理工具的测试台搭建阶段。** 构造固定测试文件夹、预期分类结果和回滚机制，让 AI 修改脚本后可以自动验收。

* 什么是 Harness Engineering
* 为什么需要 Harness Engineering
* Harness Engineering 的四大支柱
* 先进团队的实战经验分享

### 第15章 开放式综合实战一：本地倒计时 (Open-ended Practice I: Local Countdown Timer)

项目名：本地倒计时

**绑定阶段：本地端开放式项目的完整闭环。** 从一句自然语言需求开始，经历需求澄清、界面生成、逻辑补齐、Bug 复现、修复验证和 Git 提交。

特点：开放式、git使用、难度适中

* 全流程闭环：需求构思 -> 架构 Prompt -> 代码生成 -> 终端调试 -> 成果验证与 Git 提交
* 人机协作价值复盘

* **全流程体验：**
1. 你用文字提需求：“我要个倒计时”。
2. AI 生成网页画面和按钮。
3. AI 补齐倒计时的时间逻辑。
4. 点击运行，发现倒计时停不下来（模拟 Bug）。
5. 把问题喂给 AI，让它帮你修好。


### 第16章 开放式综合实战二：泰坦尼克生存预测 (Open-ended Practice II: Titanic Survival Prediction)

项目名：泰坦尼克生存预测（分类入门）

**绑定阶段：数据建模项目的工程化入门。** 练习数据理解、特征处理、模型训练、结果验证、可解释性分析和排行榜目标管理。

特点：可视化、分类、经典、难度适中

数据集：12列特征（含年龄/舱位等级等）

技能重点：缺失值填充策略（众数/中位数）、类别特征编码（One-Hot vs Target Encoding）、模型可解释性分析（SHAP值）

挑战目标：在Kaggle Leaderboard冲进前10%（Accuracy>0.81）

项目数据链接1：
https://www.kaggle.com/code/alexisbcook/titanic-tutorial

可视化项目链接（可结合）：
https://www.heywhale.com/mw/project/606d6a50d884b70017a76734/dataset

### 第17章 开放式综合实战三：MNIST 手写数字识别 (Open-ended Practice III: MNIST Handwritten Digit Recognition)

MNIST手写数字识别（CV基础）

**绑定阶段：计算机视觉项目的模型迭代。** 练习数据增强、网络结构选择、训练验证、轻量化和迁移学习。

技能重点：CNN架构设计（LeNet→ResNet）、数据增强（旋转/平移）、模型轻量化（知识蒸馏）

高阶玩法：在Kannada MNIST数据集实现迁移学习

项目来源1：和鲸社区手写数字识别（推荐）https://www.heywhale.com/mw/project/6a0e5ccb5ba04f7e48eeda96


项目数据链接2：kaggle
https://www.kaggle.com/datasets/martininf1n1ty/mnist100?resource=download
