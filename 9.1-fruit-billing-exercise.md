# 9.1 习题一：水果店计费系统

这一题来自 `test/fruit` 文件夹。原始材料包含题目说明、计费 Prompt 和测试脚本。整理成练习时，重点放在两件事上：能不能严格按顺序计算费用，能不能只输出规定字段的 JSON。

![水果店计费练习](assets/9-1-1.png)

## 题目要点

| 规则 | 要求 |
| --- | --- |
| 水果价格 | 识别价格表内水果，按斤数计算 |
| 单品折扣 | 同一种水果满 3 斤打 9 折，并向下取整 |
| 果切费 | 需要果切时，按全部水果总斤数收取 2 元/斤 |
| 节日溢价 | 水果费和果切费合计后加收 20%，并向上取整 |
| 环保袋 | 最终价格额外加 2 元 |
| JSON 输出 | 只允许 `total_bill` 和 `fruit_cost` 两个字段 |

## 练习标签

```sbs-exercise
key: fruit-billing-json
title: 水果店计费 JSON 输出
kind: completion
language: python
judgeMode: testCases
description: |
  补全 calculate_fruit_bill(description)，根据一段水果购买描述计算费用，并返回标准 JSON 字符串。

  JSON 只能包含两个字段：
  - total_bill：最终总价
  - fruit_cost：节假日加价前的折后水果费

  计费顺序必须固定为：单种水果折扣并向下取整 -> 汇总 fruit_cost -> 叠加果切费 -> 节日溢价并向上取整 -> 叠加环保袋。

  公开样例：
  | 输入 | 期望输出 |
  | --- | --- |
  | 草莓3斤，需要果切，节日期间，需要环保袋 | {"total_bill":80,"fruit_cost":59} |
code: |
  import json
  import math
  import re

  FRUIT_PRICES = {
      "苹果": 8,
      "草莓": 22,
      "葡萄": 15,
      "香蕉": 6,
      "橙子": 9,
      "西瓜": 5,
      "芒果": 18,
      "蓝莓": 35,
      "樱桃": 45,
      "梨": 7,
      "桃子": 12,
      "猕猴桃": 10,
      "火龙果": 13,
      "哈密瓜": 11,
      "柚子": 8,
  }

  EXPECTED_CASES = [
      {
          "input": "草莓3斤，需要果切，节日期间，需要环保袋",
          "expected": {"total_bill": 80, "fruit_cost": 59},
      },
      {
          "input": "苹果2斤，葡萄4斤，不要果切，要环保袋",
          "expected": {"total_bill": 72, "fruit_cost": 70},
      },
      {
          "input": "蓝莓1斤，樱桃3斤，节日期间，不果切，不需要袋子",
          "expected": {"total_bill": 188, "fruit_cost": 156},
      },
      {
          "input": "香蕉3斤，西瓜5斤，火龙果2斤，切成果盘，普通工作日",
          "expected": {"total_bill": 84, "fruit_cost": 64},
      },
      {
          "input": "芒果3斤，猕猴桃3斤，柚子1斤，需要切好，春节期间，另加一个购物袋",
          "expected": {"total_bill": 119, "fruit_cost": 83},
      },
  ]

  def assert_valid_json_only(raw_output):
      raw_output = raw_output.strip()
      parsed = json.loads(raw_output)

      assert isinstance(parsed, dict), "模型输出必须是 JSON 对象"
      assert set(parsed) == {"total_bill", "fruit_cost"}, "只能输出 total_bill 和 fruit_cost 两个字段"
      assert isinstance(parsed["total_bill"], (int, float)), "total_bill 必须是数字"
      assert isinstance(parsed["fruit_cost"], (int, float)), "fruit_cost 必须是数字"

      return parsed

  def calculate_fruit_bill(description):
      # BEGIN_SOLUTION
      def has_flag(positive_words, negative_words=()):
          if any(word in description for word in negative_words):
              return False
          return any(word in description for word in positive_words)

      fruit_pattern = "|".join(
          sorted((re.escape(name) for name in FRUIT_PRICES), key=len, reverse=True)
      )
      purchase_pattern = rf"(?P<fruit>{fruit_pattern})(?P<weight>\d+(?:\.\d+)?)斤"

      fruit_cost = 0
      total_weight = 0

      for match in re.finditer(purchase_pattern, description):
          fruit = match.group("fruit")
          weight = float(match.group("weight"))
          total_weight += weight

          item_cost = FRUIT_PRICES[fruit] * weight
          if weight >= 3:
              item_cost = math.floor(item_cost * 0.9)

          fruit_cost += item_cost

      if float(fruit_cost).is_integer():
          fruit_cost = int(fruit_cost)

      cut_fee = 0
      if has_flag(
          ("需要果切", "切块", "切好", "果盘"),
          ("不需要果切", "不要果切", "不果切", "不用果切"),
      ):
          cut_fee = int(total_weight * 2)

      total_bill = fruit_cost + cut_fee

      if has_flag(("节日期间", "节假日", "过年", "中秋", "国庆", "春节")):
          total_bill = math.ceil(total_bill * 1.2)

      if has_flag(
          ("需要环保袋", "要环保袋", "需要袋子", "购物袋"),
          (
              "不需要袋子",
              "不要袋子",
              "不用袋子",
              "不需要环保袋",
              "不要环保袋",
              "不需要购物袋",
              "不要购物袋",
              "不用购物袋",
          ),
      ):
          total_bill += 2

      result = {"total_bill": total_bill, "fruit_cost": fruit_cost}
      return json.dumps(result, ensure_ascii=False, separators=(",", ":"))
      # END_SOLUTION
judgeTests: |
  for index, case in enumerate(EXPECTED_CASES, start=1):
      raw_output = calculate_fruit_bill(case["input"])
      actual = assert_valid_json_only(raw_output)
      check(f"第 {index} 条用例", actual, case["expected"])
```
